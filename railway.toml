[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server/presence-server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[env]
PERSISTENT_DATA_DIR = "/data"

# Railway 控制台：Volumes → 挂载到本服务的 /data（与 PERSISTENT_DATA_DIR 一致）
